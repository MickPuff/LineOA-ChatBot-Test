{
  "name": "lineoa-gemini-chatbot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LINE Official Account webhook that replies with Gemini and keeps conversation context.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "@line/bot-sdk": "^10.0.0",
    "@upstash/redis": "^1.38.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
